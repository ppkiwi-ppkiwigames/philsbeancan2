# Math Time + Wisp

This version runs Math Time behind an Ultraviolet frontend and uses Mercury Workshop's public Wisp server as the transport:

`wss://wisp.mercurywork.shop/`

## Run

```bash
npm install
npm start
```

Open `http://localhost:8080`.

For deployment, use HTTPS. Service workers require HTTPS outside localhost.
